import React from 'react';
import PropTypes from 'prop-types';
import { FooterContainer } from './style';

const Footer = () => {
  return (
    <FooterContainer>
      <span>Built with passion by STC</span>
    </FooterContainer>
  );
};

export default Footer;
